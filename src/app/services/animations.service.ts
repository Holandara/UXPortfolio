// Utility service — thin wrapper kept for future use.
// Direct animate/inView calls in components are preferred.
import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class AnimationsService {}
